name: istperegrination
services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:80"
    volumes:
      - "./backend:/var/www/html"
    env_file:
      - ".env"
    command: bash -c "a2enmod rewrite && apache2-foreground"
